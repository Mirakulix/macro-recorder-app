<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Overlay widget (floating stop button visible over all apps) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Foreground services -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Notifications (required on Android 13 / API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Storage for macro export / import -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- Wake lock keeps recording reliable in background -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Restore scheduled macros after reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Exact alarm for timed macro execution -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:name=".MacroRecorderApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MacroRecorder">

        <!-- ── Activities ─────────────────────────────────────────────── -->

        <activity
            android:name=".presentation.main.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".presentation.detail.MacroDetailActivity"
            android:exported="false"
            android:parentActivityName=".presentation.main.MainActivity"
            android:windowSoftInputMode="adjustResize" />

        <!-- ── Accessibility Service ──────────────────────────────────── -->
        <!--
            Captures touch events during recording and dispatches gestures
            during macro playback via dispatchGesture().
        -->
        <service
            android:name=".service.accessibility.MacroRecorderAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- ── Foreground Services ────────────────────────────────────── -->

        <!-- Runs while the user is recording a macro -->
        <service
            android:name=".service.recording.RecordingForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Manages the draggable overlay widget -->
        <service
            android:name=".service.overlay.OverlayWidgetService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Runs during macro playback -->
        <service
            android:name=".service.execution.ExecutionForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- ── Broadcast Receivers ────────────────────────────────────── -->

        <!-- Restores scheduled macros after device reboot -->
        <receiver
            android:name=".service.execution.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Fires when a scheduled macro alarm triggers -->
        <receiver
            android:name=".service.execution.ScheduledMacroReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.macrorecorder.app.RUN_SCHEDULED_MACRO" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
